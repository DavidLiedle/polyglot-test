#include <stdio.h> // standard I/O declarations

// Verifies that the C compiler runs.

int main() { // program entry point
  // \n adds a newline after the text
  printf("Output from C!\n");
  return 0;
}

// Compile with: gcc test.c -o test && ./test
